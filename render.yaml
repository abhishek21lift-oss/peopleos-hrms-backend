services:
  - type: web
    name: peopleos-hrms-backend
    runtime: node
    plan: free
    repo: https://github.com/abhishek21lift-oss/peopleos-hrms-backend
    branch: main
    buildCommand: npm install && npm run build
    startCommand: node src/server.js
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
